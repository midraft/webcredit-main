package com.example.application.data.service;

public class ClientDAOImpl {

/*    @Override
    public Client getClientById(UUID CLIENTID) {
        SqlSession sqlSession = SqlSessionFactoryUtils.getSqlSessionFactory().openSession();
        Client user = sqlSession.selectOne("client.getUserById", UUID);
        sqlSession.close();
        return user;
    }

    @Override
    public List<Client> getClientByClientName(String NAME) {
        SqlSession sqlSession = SqlSessionFactoryUtils.getSqlSessionFactory().openSession();
        List<Client> list = sqlSession.selectList("client.getClientByName", Name);
        sqlSession.close();
        return list;
    }

    @Override
    public void insertClient(Client client) {
        SqlSession sqlSession = SqlSessionFactoryUtils.getSqlSessionFactory().openSession();
        sqlSession.insert("client.insertClient", client);
        sqlSession.commit();
        sqlSession.close();
    }*/
}
